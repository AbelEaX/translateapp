// In a constants or configuration file (e.g., constants.dart)

// The default database ID is typically not explicitly needed, but included for clarity.
const DEFAULT_FIRESTORE_DB_ID = '(default)';

// This is the ID of the new 'gotranslate' database instance you created.
const TRANSLATION_FIRESTORE_DB_ID = 'gotranslate';